# Promoted Warm Storage

Use this folder for promoted short-term memory blocks that are too long for `MEMORY.md`.

Rules:

- Move long promoted blocks here unchanged.
- Keep source comments and source pointers.
- Leave only a short source-linked index in `MEMORY.md`.
- Distill durable content later into leaves, topics, or digests after review.
